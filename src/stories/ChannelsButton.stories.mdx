import { Meta, Canvas, Story } from '@storybook/addon-docs';
import { action } from '@storybook/addon-actions';

import ChannelsButton from '../components/ChannelsButton';
import LightThemeIcon from '../assets/light-theme-icon.svg';
import DarkThemeIcon from '../assets/dark-theme-icon.svg';

<Meta
  title="components"
/>

export function MockPopover({ title, children }) {
  return <div title={title}>{children({})}</div>;
}
export function MockToolbarButton({ onClick, icon }) {
  return <button type="button" onClick={onClick}>{icon}</button>;
}
global.APP = {};

<Canvas>
  <Story name="ChannelsButton">
    <ChannelsButton
      Popover={MockPopover}
      ToolbarButton={MockToolbarButton}
      toggleSidebar={action('click')}
      LightThemeIcon={LightThemeIcon}
      DarkThemeIcon={DarkThemeIcon}
    />
  </Story>
</Canvas>
